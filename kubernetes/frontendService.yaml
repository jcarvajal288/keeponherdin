apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer
  ports:
    - name: frontend-service
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: keeponherdin